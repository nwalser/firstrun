/**
 * The only place the product name is allowed to live in code.
 *
 * Renaming the product should be: this constant, the repo name, and the
 * `@firstrun/*` package names. Nothing else. Do not inline the name anywhere.
 */
export const PRODUCT_NAME = "firstrun";
