{
  "name": "@firstrun/web-tag",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "check-size": "bun run build.ts --check"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
